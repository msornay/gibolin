name: CI
on: [push, pull_request]
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install ruff
      - name: Run Ruff
        run: ruff check --output-format=github .

  test-api:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run API tests
        run: docker compose run --rm api python manage.py test cave.tests

  # TODO: Fix e2e tests - Playwright version mismatch between Docker image and npm package
  # test-e2e:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: Start services
  #       run: docker compose up -d api ui postgres
  #     - name: Wait for services
  #       run: |
  #         timeout 60 bash -c 'until curl -s http://localhost:3000 > /dev/null; do sleep 2; done'
  #         timeout 60 bash -c 'until curl -s http://localhost:8000/api/refs > /dev/null; do sleep 2; done'
  #     - name: Run e2e tests
  #       run: docker compose run --rm -e BASE_URL=http://ui:3000 e2e
  #     - name: Stop services
  #       if: always()
  #       run: docker compose down
